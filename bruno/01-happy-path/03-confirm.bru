meta {
  name: Confirm the membership
  type: http
  seq: 3
  tags: [
    pattern
  ]
}

post {
  url: {{baseUrl}}/api/memberships/confirm/{{membershipId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
