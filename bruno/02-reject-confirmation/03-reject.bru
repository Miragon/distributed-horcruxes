meta {
  name: Reject the confirmation
  type: http
  seq: 3
  tags: [
    pattern
  ]
}

post {
  url: {{baseUrl}}/api/memberships/reject/{{membershipId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
